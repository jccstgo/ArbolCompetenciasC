# Árbol de Competencias (React + Vite)

## Requisitos
- Node.js 18+ (recomendado 20+)

## Ejecutar en modo desarrollo
```bash
npm install
npm run dev
```

Luego abre la URL que imprime Vite (normalmente `http://localhost:5173`).

